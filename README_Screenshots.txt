SCREENSHOT REQUIREMENT
The official project requires screenshots of SQL query outputs.
The included PNGs are formatted query/evidence templates only, because the three official CSV datasets were not provided in this chat.

To finalize:
1. Import data_science_team.csv, proj_table.csv and emp_record_table.csv into database employee.
2. Run each SQL file in the 02_SQL_Code folder.
3. Capture the SQL editor + result grid.
4. Replace the corresponding Task_XX template PNG.
5. For Task 10, capture EXPLAIN before index and EXPLAIN after index.
Do not submit fabricated output rows.
